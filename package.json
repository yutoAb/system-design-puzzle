{
  "name": "system-design-puzzle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test 'test/**/*.test.js'",
    "dev": "concurrently -n api,web -c blue,green \"node --env-file-if-exists=.env src/server.js\" \"vite\"",
    "build": "vite build",
    "start": "node --env-file-if-exists=.env src/server.js",
    "preview": "npm run build && npm start"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.7",
    "@xyflow/react": "^12.11.2",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "stripe": "^22.3.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.3",
    "concurrently": "^10.0.3",
    "vite": "^8.1.5"
  }
}
